<!--index.wxml-->
<view class="container">
  <swiper indicator-dots="{{false}}"
  autoplay="{{autoplay}}">
  <block wx:for="{{imgUrls}}" wx:key="*this">
    <swiper-item>
      <image src="{{item}}" class="slide-image" mode="{{imageMode}}"/>
    </swiper-item>
  </block>
</swiper>
</view>
